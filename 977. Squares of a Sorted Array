class Solution {
    public int[] sortedSquares(int[] A) {
    int n = A.length;
        int[]ans = new int[n];
        for(int i=0;i<n;i++){
            ans[i] = A[i]*A[i];
        }
        Arrays.sort(ans);
        return ans;
    }
}
time complexity-o(nlogn)
space - o(n)
